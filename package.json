{
  "name": "pennypilot",
  "version": "1.0.0",
  "description": "PennyPilot - household finance tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "node --watch src/server/index.js",
    "dev:client": "npm --prefix src/client run dev",
    "build:client": "npm --prefix src/client run build",
    "start": "node src/server/index.js",
    "install:client": "npm --prefix src/client install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
